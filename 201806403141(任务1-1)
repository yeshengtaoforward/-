#include<stdio.h>
void main()
{
	while(1)
	{
		int i,s,f;
		int a[10]={1,3,5,8,10,13,14,18,19,34};
		printf("Enter:");
		scanf("%d",&s);
		f=0;
		for(i=0;i<10;i++)
		{
			if(s==a[i]);
			printf("第%d个数\n",i+1);f=1;
		}
		if(f==0) printf("没有这个数，请再次输入：");
	}
}
